<p id="notice"><%= notice %></p>

<p>
  <strong>Beer club:</strong>
  <%= @membership.beer_club_id %>
</p>

<p>
  <strong>User:</strong>
  <%= @membership.user_id %>
</p>

<%= link_to 'Edit', edit_membership_path(@membership), class:"btn btn-primary" if current_user %> |
<%= link_to 'Back', memberships_path, class:"btn btn-default" %>

<h1>New Membership</h1>

<%= form_for(@membership) do |f| %>
  <%= f.select :beer_club_id, options_from_collection_for_select(@beerclubs, :id, :to_s) %>
  <%= f.submit class:"btn btn-primary"%>
<% end %>
<%= link_to 'Back', memberships_path, class:"btn btn-default" %>

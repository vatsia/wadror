<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th>Average</th>
  </tr>
  </thead>

  <tbody>
  <% items.each do |item| %>
      <tr>
        <td><%= link_to item, item %></td>
        <td><%= round(item.average_rating) %></td>
        <td></td>
      </tr>
  <% end %>
  </tbody>
</table>